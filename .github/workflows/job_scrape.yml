name: Job Feed Scrape

on:
  schedule:
    - cron: '0 7 * * *' # Run daily at 3:00 AM EDT (7:00 AM UTC) — scraping has no DeepSeek cost
  workflow_dispatch: # Allow manual runs, scoped to one engine or all of them
    inputs:
      engine:
        description: 'Engine to scrape (all = every engine, the full production run)'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - successfactors
          - workday
          - njoyn
          - oracle
          - dayforce
          - jobs2web
          - icims
          - hrsmart
          - ultipro
          - adp
          - taleo
          - avanti
          - bamboohr
          - talentpoolbuilder
          - custom
          - rss
          - jazzhr
          - workland
          - jibe
          - peoplesoft
          - csod
          - technomedia

jobs:
  setup:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      engines: ${{ steps.set.outputs.engines }}
    steps:
      - id: set
        run: |
          if [ "${{ github.event_name }}" = "schedule" ] || [ "${{ inputs.engine }}" = "all" ] || [ -z "${{ inputs.engine }}" ]; then
            echo 'engines=["successfactors","workday","njoyn","oracle","dayforce","jobs2web","icims","hrsmart","ultipro","adp","taleo","avanti","bamboohr","talentpoolbuilder","custom","rss","jazzhr","workland","jibe","peoplesoft","csod","technomedia"]' >> "$GITHUB_OUTPUT"
          else
            echo 'engines=["${{ inputs.engine }}"]' >> "$GITHUB_OUTPUT"
          fi

  scrape:
    needs: setup
    strategy:
      fail-fast: false
      matrix:
        engine: ${{ fromJSON(needs.setup.outputs.engines) }}
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: scraper/package-lock.json

      - name: Install dependencies
        run: cd scraper && npm ci

      - name: Install Playwright Browsers
        run: cd scraper && npx playwright install chromium --with-deps

      - name: Run Scraper headed (${{ matrix.engine }})
        if: matrix.engine == 'njoyn' || matrix.engine == 'jobs2web'
        env:
          NEON_CURRENT_DATABASE_URL: ${{ secrets.NEON_CURRENT_DATABASE_URL }}
          NEON_ARCHIVE_DATABASE_URL: ${{ secrets.NEON_ARCHIVE_DATABASE_URL }}
          TURSO_URL: ${{ secrets.TURSO_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
          SCRAPE_ENGINE: ${{ matrix.engine }}
          REFRESH_PARSED_DETAIL_PAGES: ${{ matrix.engine == 'workday' && 'true' || 'false' }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: cd scraper && xvfb-run --auto-servernum --server-args="-screen 0 1280x800x24" npx tsx scraper.ts

      - name: Run Scraper (${{ matrix.engine }})
        if: matrix.engine != 'njoyn' && matrix.engine != 'jobs2web'
        env:
          NEON_CURRENT_DATABASE_URL: ${{ secrets.NEON_CURRENT_DATABASE_URL }}
          NEON_ARCHIVE_DATABASE_URL: ${{ secrets.NEON_ARCHIVE_DATABASE_URL }}
          TURSO_URL: ${{ secrets.TURSO_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
          SCRAPE_ENGINE: ${{ matrix.engine }}
          REFRESH_PARSED_DETAIL_PAGES: ${{ matrix.engine == 'workday' && 'true' || 'false' }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: cd scraper && npx tsx scraper.ts

  promote:
    needs: scrape
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: scraper/package-lock.json

      - name: Install dependencies
        run: cd scraper && npm ci

      - name: Promote readable scraped postings
        env:
          NEON_CURRENT_DATABASE_URL: ${{ secrets.NEON_CURRENT_DATABASE_URL }}
          NEON_ARCHIVE_DATABASE_URL: ${{ secrets.NEON_ARCHIVE_DATABASE_URL }}
          TURSO_URL: ${{ secrets.TURSO_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
        run: cd scraper && npm run backfill-metadata-only -- --apply
